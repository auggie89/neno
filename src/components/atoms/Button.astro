---
import RightArrow from "../Icons/RightArrow.astro";
const {class:className, type, href,} = Astro.props;
const Element = href ? "a" : "button"
---

<Element class:list={[className,"ga-btn"]} type={type} href={href}>
  <slot /> 
  <div class="ga-btn__icon">
    <RightArrow width="15" height="15"/>
  </div>
</Element>

<style>
  .ga-btn {
    display: flex;
    align-items: center;
    gap: 3rem;
    color:#fff;
    font-weight: 700;
    border-radius: 50px;
    border: none;
    font-size: 1.25rem;
    padding: 0.875rem 1.25rem 0.875rem 1.875rem;
    box-shadow: rgba(51,51,51,0.2) 0 4px 0;
  }
  
  .ga-btn-primary {
    background-color: #D51E2D;
  }
  .ga-btn-secondary {
    background-color: #20262E;
  }
  
  .ga-btn__icon {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 10px;
    border-radius: 50%;
    border: solid white 2px;
  }
  .ga-btn__icon:hover {
    background-color: #2DA0AB;
  }
</style>